# Pending workflows

These files belong in `.github/workflows/`. They were parked here because the
session that authored the branch could not push workflow files (no `workflow`
OAuth scope). Move them with the GitHub web editor (edit → change the path to
`../workflows/<name>`), deleting the old `.github/workflows/pr.yml` first, then
delete this folder.
